# FkstClient v1.9.0 —— 试卷库（测试版）

版本号 `1.9.0`（versionCode 10）。

> ⚠️ **测试版（pre-release）**：试卷库是刚挖出来的一整块新功能，接口那边的坑还比较多
> （题目只有同步卷能读、收藏接口的返回值不可信、在线交卷仍然做不到），
> 先发出来试。遇到显示不对或者点不动的地方，麻烦在 Issues 里说一下。

这次的核心是把「刷题」那块重新挖了一遍，结果发现**题目其实是拿得到的**，
于是有了底部导航第一位的**「试卷」板块（试卷库）**。

---

## 新增：试卷库（底部导航「试卷」）

- **浏览真实试卷**：`GetShuatiPaper5`，分页加载，卡片显示封面、标题、类型、题量、是否含答案
- **按年级筛选**：一年级 ~ 六年级、七年级/八年级（数学·英语）、九年级（化学）、高中数学
- **按教材版本筛选**：版本列表随年级动态拉取（`GetSTFilterData`），如人教版 / 外研版 / 沪教版…
- **搜试卷**：`GetSearchPapers7`（comment 签名变体），搜标题关键词
- **收藏试卷**：`CollectShuatiPaper`（status + type + pid）
  ⚠️ 说明白这个接口的坑：它**恒回 `res=0`**，连不存在的 pid 也是 0，没法用返回值判断成败；
  首次实测可逆（收藏 → 收藏列表出现，取消 → 消失），但复测遇到过「返回 0、列表里查不到」
  （同一时段笔记收藏列表、赞过列表也一起 `res=1`，像是服务端侧抖动）。
  所以客户端是**乐观更新**本地的已收藏集合，进页面时拉一次收藏列表校正。
- **我收藏的试卷**：顶栏星标入口，单独的列表页

## 新增：试卷详情（题目 + 答案 + 解析）

`GetZJPaperById5`（pid + paperid + type + aid）返回整份卷子：

- 按**大题分组**展示（如「完形填空」「语法选择」）
- 每题显示题干、选项
- 顶部「显示答案与解析」开关：默认关着，可以先自己做完再一次性展开对答案
- 答案与解析带底色区分

> 只对 `type=1`（中小学同步卷）有效。考研（22）、专升本（14）、教资（1111/1112）这些
> 服务端一律回 `res=1`，列表里照常显示、点进去会说明「暂不支持在线查看题目」。

## 变化

- 底部导航从四项变五项：**试卷 / 发现 / 搜索 / 私信 / 我的**（试卷在第一位）
- 首次启动的说明页补了一段：讲清楚为什么没有刷题模块、以及现在改成了什么

---

## 技术要点（都写进 `API_RESEARCH.md` 第 18 节了）

1. **`GetZJPaperById5` 翻案**：之前判定它「不存在/坏了」，其实只是参数没给全 ——
   服务端会依次提示缺 `type` → `aid` → `paperid`，补齐后直接返回 `questionlist`。
2. **`GetShuatiPaper5` 只有两个筛选参数生效**：`f_gradeid`（年级）和 `version_id`（版本），
   而且 `version_id` 必须跟 `f_gradeid` 一起传，单独传恒回 0 条。
   `subject` / `xd` / `papertype` 传了会被服务端直接忽略。
3. **三个接口的响应里没有 `res` 字段**（`GetShuatiPaper5` / `GetZJPaperById5` / `GetSearchPapers7`），
   调用时必须跳过 res 校验，否则会被当成失败。
4. **搜试卷用的是 comment 签名变体**，跟搜笔记同款；用通用签名会回「url illegal！」。
5. 年级（`f_gradeid`）映射是**实测校准**的：拿每个值拉一页，按返回卷子的标题反推；
   对不上号的（17 / 25 / 27）没有放进筛选表。

## 仍然做不到的（说明白，免得误会）

- **在线交卷**：答题页是官方 H5（`questionExercise-v17` / `paperExercises-v18` / `simpleUsePaper`），
  对第三方客户端恒回「非法访问-1」，服务端也没开放提交答题记录的接口。
  所以它是「查卷子 + 看题看解析」的工具，不是刷题机。
- **非同步卷的题目**（见上）
- **下载试卷 PDF**：`GetSTPaperPdfUrl` 要 `did`，试过试卷 id 和收藏 link_id 都是 `res=2`，没查出来

---

## 安装包

- 文件：`release/FkstClient-v1.9.0-release.apk`
- 体积：10,943,666 字节（约 10.4 MB）
- SHA256：`705f1b1a5b9ad692b523e95613e84940550cf9a6e9b38d6ef9b7255cb884fcf6`
- Android 7.0（API 24）及以上，arm64 / arm 通用
- 覆盖安装即可，登录态保留
