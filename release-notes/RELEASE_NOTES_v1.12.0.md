# FkstClient v1.12.0（正式版）

第三方「疯狂刷题」客户端。这是第一个**正式版**（此前 v1.9.0 ~ v1.11.5 都是测试版），
把「试卷 / 刷题」这条线走通了：能看题、能在线刷、能在 H5 不给题面时照样刷。

## 相对 v1.11.3 的变化

### 1. 实时练习修好了（v1.11.4 / v1.11.5）
- **v1.11.3**：补上官方 H5 的合法会话（`loginToken` / `tokenSeed` 走 YEX 加密登录通道），
  终于能进官方答题页。
- **v1.11.4**：修「进去了但题干空白」—— 官方 `paperExercises` 页面内的 ajax 需要
  `yex_session` 会话 cookie，直载答题页只种 XSRF-TOKEN。改成两段式加载：先静默访问
  官网首页把会话 cookie 种下来，再加载答题页。
- **v1.11.5**：修「骨架在、题目不在」的根因。实测发现官方页面**只对部分试卷给完整题面**：
  - 正常卷 32~34 万字节，页内带 `shuati-fun-v1.js` 渲染引擎，题干内联；
  - 「答案暂缺」那类卷只有 54KB，页里只有题目 ID、没有渲染引擎，题干按官方设计要由
    宿主 App 注入（`getCacheData()`）。
  所以这类卷在**任何**第三方客户端里都是空壳。现在练习页加载完会探测一次页面有没有题面，
  没有就**自动切到「原生刷题」**：题目 / 选项 / 答案 / 解析从试卷接口取，支持点选项作答、
  对答案、看解析、上一题 / 下一题、进度显示；顶栏可手动切回官方 H5。

### 2. 去掉「为什么没有刷题模块」说明页
- 首次启动引导只剩用户政策一步（勾选同意才能继续）。
- 刷题已经做出来了，那段「解释为什么没有刷题」的说明不再需要，整页（含持久化标记）删除。

## 主要能力

- **试卷库**：按年级 / 教材版本浏览真实试卷，搜试卷、收藏，点进去看整份卷子的题目 + 答案 + 解析。
- **实时练习**：试卷详情页 ▶ 按钮，用本账号合法会话拼官方 `paperExercises-v18` 页面丢进
  WebView，**能真答题、真交卷**；官方不给题面的卷自动转原生刷题。
- **我的答题记录**：回看做过的题（GetRecordShuatiQuestion1），上一题 / 下一题翻页。
- **社区**：发现流（11 分区 + 关注流）、搜索、私信（防撤回 / 置顶 / 发图）、
  发布笔记（最多 9 图 + 3 段音频）、编辑 / 删除自己的笔记、收藏、投币。
- **通知**：按动作类型（评论 / 点赞 / 投币）分组展示，点击直达原文；评论区支持发图。
- **其它**：更新检查（GitHub Releases）、单列 / 双列排版切换。

## 已知限制（诚实说明）

- 实时练习走的是官方 H5，体验取决于官方页面本身（样式、个别机型 WebView 兼容性）。
- 原生刷题的作答只存在本地，不写回服务端；要留答题记录请用官方 H5（有题面的卷子）。
- 答题记录接口只返回题干 + 选项，不含对错。
- 评论发图底层 `content_url` 仍未在真机端到端验证（测试账号被限言）。

## 安装

下载 `FkstClient-v1.12.0-release.apk` 安装即可（覆盖安装不会丢登录态）。

- APK SHA256 见 Release 页说明。
