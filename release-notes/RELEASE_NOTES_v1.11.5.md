# v1.11.5（测试版）

官方 H5 无题面时，自动切到原生刷题。

## 定位

官方 `paperExercises` 页面只对部分试卷给完整题面。像「答案暂缺」那类卷子，
服务端返回的是降级版页面——只有题目 ID、不带 `shuati-fun` 渲染引擎，
题干要宿主 App 通过 `getCacheData()` 注入（实测该卷 54KB，正常卷 32 万+ 字节）。
所以任何第三方客户端进去都是一片空白，不是加载姿势问题。

## 改动

练习页加载完会探测一次页面有没有题面（有无 `ShuatiMathod` 引擎 + 正文长度），
没题面就自动切到「原生刷题」——题目数据走 `GetZJPaperById5`（题干/选项/答案/解析齐全），
支持选题作答、对答案、看解析、上一题/下一题、进度显示；顶栏可手动切回官方 H5。

## 实测

真机模拟器：自动切换生效、题干正常显示、选项 A/B/C/D 可点选、
对答案显示「答案：B ✓ 答对」、答案与解析正常。

## 校验

- 包名：`com.tb.fkst`
- versionCode 17 / versionName 1.11.5
- 体积：10,994,694 字节
- SHA256：`a68f46720800b5c86893914a4054d8fe43b3d29b69e00575c189cca8b19f0e85`
