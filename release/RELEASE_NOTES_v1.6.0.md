本次修复了正文显示问题，并新增笔记音频功能。

---

### 🐛 修复

**自己发的笔记，正文显示成一串 JSON**

之前发布时把正文包成了 `{"version":1,"text":…}` 这样的 JSON 提交，
而服务端其实是把 `content` 当**普通正文文本**存的，于是那串 JSON 被原样当成了正文；
再经过服务端自己的包装，读回来就成了嵌套两层的结构，详情页会把整坨 JSON 显示出来。

现在改为**直接提交纯文本**。已经发出去的旧笔记也不用删 ——
客户端做了兼容（会反复解包再剥标签），旧笔记不会再显示 JSON。

**列表里的正文摘要偶发带着标签**

正文归一化逻辑一起加固了：先解包体 JSON，再剥 HTML 标签，且支持多层嵌套。

---

### ✨ 新功能

**笔记音频**

发布页新增「选音频」，一篇最多 3 段，**只支持 mp3**。
音频会附在正文末尾，在文章详情页渲染成播放器：播放 / 暂停、进度条、
时长显示，同一条笔记里只会有一路音频在响。

> **说明**：服务端的笔记正文并没有音频字段（扫过 11 个分区共 363 篇社区笔记，
> 零音频痕迹），所以这是本客户端自己的扩展 —— 音频在正文里以一行
> `[音频] <地址>` 表示。我们这边渲染成播放器，
> **官方客户端会显示成一行普通文字**，不影响阅读。

---

### 📦 下载

| 项目 | 值 |
|---|---|
| 文件 | `FkstClient-v1.6.0-release.apk` |
| 大小 | 10.4 MB（10,869,598 字节） |
| 版本 | versionName 1.6.0 / versionCode 7 |
| 系统要求 | Android 7.0（API 24）及以上 |
| SHA256 | `ca7593ebe78b086925a71c8468e3a2548cd2520b928de568394f3f3d8dfcc0ad` |

校验方式：

```bash
# Windows (PowerShell)
Get-FileHash .\FkstClient-v1.6.0-release.apk -Algorithm SHA256

# macOS / Linux
shasum -a 256 FkstClient-v1.6.0-release.apk
```

---

### ⚠️ 安装说明

- 从 v1.4.x / v1.5.x 升级可直接**覆盖安装**，签名未变更
- 安装包使用自签证书，首次安装系统会提示「未知来源应用」，请手动允许
- 私信功能受平台规则限制：服务端要求账号**连续签到满 3 天**才允许对外发私信，
  官方客户端同样受限，非本项目问题

---

### 📄 免责声明

本项目仅供学习与技术研究，与「疯狂刷题」官方及其开发方无任何关联。
请勿用于商业用途、批量刷量或骚扰他人等违反平台协议的行为。
使用产生的一切后果由使用者自行承担，**建议使用小号测试**。

完整功能清单见仓库 `README.md` 与 `android/README.md`，
协议与接口细节见 `API_RESEARCH.md`。
